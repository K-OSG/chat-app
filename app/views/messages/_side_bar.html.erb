<div class="side-bar-header">
  <div class="header-name">
  <%# link_to リンク先の表示文字, 行き先のパス %>
  <%# current_user.nameでログインしているユーザー名
  edit_user_path(current_user)でログインしているユーザーの詳細情報へ %>
    <%= link_to current_user.name, edit_user_path(current_user) %>
  </div>
  <div class="create-room">
    <%# <%= link_to "チャットを作成する", new_room_path %> %>
    <%= link_to "チャットを作成する", new_room_path %>
  </div>
</div>

<div class="rooms">
  <div class="room">
    <div class="room-name">
    <%# current_user.roomsにてログインユーザーのroomsテーブルのデータを取得して並べる %>
  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <a href="#"><%= room.name %></a>
      </div>
    </div>
  <% end %>
  </div>
</div>